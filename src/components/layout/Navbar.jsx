// Este componente puede ser eliminado ya que la navegación está en Header.jsx
// O puedes usar este para una navegación específica si la necesitas

const Navbar = () => {
    return (
        <nav>
            {/* Si necesitas una navegación específica adicional */}
        </nav>
    );
};

export default Navbar;